{
  "name": "@yge/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "dev": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "build": "echo 'Mobile build runs through EAS — see eas.json when set up'",
    "lint": "echo 'lint TBD'",
    "typecheck": "node -e \"require('fs').existsSync('node_modules/expo') ? require('child_process').execSync('tsc --noEmit',{stdio:'inherit'}) : console.log('mobile: skipping tsc until pnpm install runs (expo not yet installed)')\"",
    "test": "echo 'mobile tests TBD'"
  },
  "dependencies": {
    "@yge/shared": "workspace:*",
    "expo": "~50.0.0",
    "expo-constants": "~15.4.0",
    "expo-status-bar": "~1.11.1",
    "react": "18.2.0",
    "react-native": "0.73.6",
    "react-native-safe-area-context": "4.8.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@types/react": "~18.2.0",
    "typescript": "^5.4.0"
  }
}
